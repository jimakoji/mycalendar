$('#admin_info').html( "<%= escape_javascript(render 'edit') %>" ); 
$('#admin_info_dialog').dialog({
        title: 'Edit Information / お知らせ編集',
        modal: true,
        position: {
                 of: '.fc-header',
                 my: "center top",
                 at: "center bottom"
                },
        width: 500,

        close: function(event, ui) { 
                 $('#event_dialog').dialog('destroy');
                 admin_info_dialog_reset();
               }
});