if ($('#error_explanation_js')!=null) { $('#error_explanation_js').remove(); }

$('#event_info').html( "<%= escape_javascript(render 'edit_form') %>" )

$('#event_dialog').dialog({
        title: "edit / 編集",
        modal: true,
        position: {
                 of: '.fc-header',
                 my: "center top",
                 at: "center bottom"
                },
        width: 660,

        close: function(event, ui) { 
                 $('#desc_dialog').dialog('destroy');
                 event_dialog_reset();
               }
});

<% if @file_error_dialog %>
  alert('<%= escape_javascript @file_error_dialog %>');
<% end %>
