$('#user_info').html( "<%= escape_javascript(render 'edit_form') %>" ); 
$('#user_dialog').dialog({
        title: 'Update your profile / ユーザープロファイル更新',
        modal: true,
        position: {
                 of: '.fc-header',
                 my: "center top",
                 at: "center bottom"
                },
        width: 660,

        close: function(event, ui) { 
                 $('#user_dialog').dialog('destroy');
                 user_dialog_reset();
               }
});
